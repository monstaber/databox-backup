{
  "name": "databox-backup",
  "version": "0.1.0",
  "private": true,
  "description": "Personal archiver for the Czech datová schránka (ISDS). Every hour a GitHub Actions workflow pulls new/changed messages from ISDS and stores them (as signed ZFO + signed doručenka) in Google Drive.",
  "license": "PolyForm-Noncommercial-1.0.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "test": "node --test --test-reporter=spec dist/tests/*.test.js",
    "start": "node dist/src/index.js",
    "bootstrap:google": "node dist/scripts/google-oauth-bootstrap.js",
    "verify": "npm run build && npm run lint && npm test && npm audit --audit-level=low"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "@types/node": "^24.0.0",
    "eslint": "^9.14.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.14.0"
  }
}
